# Auditoría — interpretación de la corrida de U2 contra contrato congelado

TARGET_WORK_REPO=https://github.com/francogg89-ai/work-claude-i
TARGET_WORK_SHA=1c0613b14fc19fb06c2d590990360a2ffec9b1c5

WORK_ID=revolutions-orchestra-ai
CARRIL=I

PERIMETRO_ULTIMA_MODIFICACION=CONSTITUCION

## Derivación e identidad

La entrega fue reconstruida directamente desde Git.

- corte anterior de work: `b2a7c472732e5da59b8c82da7278a0e66ed26e93`;
- corte audit recibido: `9679ca4ca6987a3706a0b06cebd3b03cce1dcc7a`;
- entrega auditada: `1c0613b14fc19fb06c2d590990360a2ffec9b1c5`;
- relación: un commit adelante y cero atrás respecto del work anterior;
- delta confinado íntegramente a `u2-reglas-orquestador/`;
- `REGLAS-ORQUESTADOR.md` conserva el blob congelado
  `49ad9e04a6ea2f04e4ec6f1f0efd2c5adf51f367`;
- al corte audit recibido no existía todavía
  `auditorias/1c0613b14fc19fb06c2d590990360a2ffec9b1c5.md`.

Archivos nuevos de la verificación:

- `u2-reglas-orquestador/verificador/EVIDENCIA.md`;
- `u2-reglas-orquestador/verificador/autoridad.py`;
- `u2-reglas-orquestador/verificador/corpus.py`;
- `u2-reglas-orquestador/verificador/orquestador.py`;
- `u2-reglas-orquestador/verificador/salida.txt`;
- `u2-reglas-orquestador/verificador/verificar.py`.

Además se modificó `u2-reglas-orquestador/EVENTO.md`.

## Contrato aplicable

La corrida se interpreta exclusivamente contra el contrato congelado en:

`audit-chatgpt-i@9679ca4ca6987a3706a0b06cebd3b03cce1dcc7a:auditorias/b2a7c472732e5da59b8c82da7278a0e66ed26e93.md`.

No se redefinen E1-E4, F1-F5, N1-N9 ni las limitaciones.

El contrato exige, entre otras cosas:

- E1: cada caso produce el resultado normativo esperado;
- E2: cada rechazo se atribuye a una regla concreta del candidato;
- E3: **toda regla mecánica del candidato** tiene al menos un caso que la ejercita;
- E4: P-C coincide con la derivación Git independiente;
- F3: ocurre si **alguna regla mecánica del candidato queda sin caso**.

Éxito exige E1-E4 simultáneamente. No existe tercera salida.

## Evidencia inspeccionada directamente

Se inspeccionaron desde el corte exacto:

- candidato `REGLAS-ORQUESTADOR.md`;
- `EVENTO.md`;
- mecanismo `orquestador.py`;
- extracción de autoridad `autoridad.py`;
- corpus `corpus.py`;
- evaluador `verificar.py`;
- salida literal `salida.txt`;
- `EVIDENCIA.md`;
- REVOLUTIONS autoritativo
  `revolutions-orchestra-ai@e05b24cc501ce839ffabee6d9666d069e056255c:metodo/REVOLUTIONS.md`.

El valor `VEREDICTO=EXITO` producido por el mecanismo se trata como resultado reportado de la corrida, no como veredicto auditor.

## Interpretación contra el contrato congelado

### E1 — casos ejecutados

La salida literal preserva 34 casos marcados `OK`, y el evaluador compara la traza producida con el resultado esperado después de ejecutar cada caso.

No se encontró en la evidencia preservada un caso ejecutado cuya traza difiera de su esperado.

E1 queda satisfecho **para los casos efectivamente incluidos en el corpus**.

### E2 — atribución de rechazos

Los rechazos ejercitados se expresan como `DETENER_REPORTAR` con una regla declarada en la lista usada por el mecanismo.

No se observa un rechazo ejercitado sin atribución.

E2 queda satisfecho para el corpus ejecutado.

### E3 — cobertura de toda regla mecánica del candidato

E3 **NO queda satisfecho**.

El mecanismo informa:

`reglas declaradas 28, reglas ejercitadas 28`.

Pero ese denominador no se deriva ni se comprueba contra el candidato. Es la colección `REGLAS` escrita manualmente dentro de `orquestador.py`, y el verificador compara la cobertura del corpus contra esa misma colección.

Eso permite que una regla mecánica real del candidato sea omitida simultáneamente de `REGLAS` y del corpus sin que el control 28/28 lo detecte.

Existe al menos una omisión comprobada directamente.

El candidato, en §1.1 `Arranque externo`, declara mecánicamente:

1. recibir el paquete de constitución aprobado;
2. abrir una instancia inicial de AUDITOR;
3. entregar ese paquete literalmente;
4. recibir la salida y entrar en el loop ordinario.

El propio `orquestador.py` contiene un método `arranque_externo`, pero:

- la colección `REGLAS` no representa como regla de cobertura esa secuencia de arranque y entrega literal del paquete;
- `verificar.py:ejecutar_caso` sólo admite operaciones `recibir`, `despachar`, `detener` y `continuar`;
- no admite una operación que invoque `arranque_externo`;
- el corpus no contiene un caso que ejercite esa secuencia.

Por lo tanto existe una regla mecánica del candidato que queda sin caso.

RESULTADO_E3=NO_SATISFECHO
F3=OCURRIO

Este hallazgo usa exactamente el criterio congelado; no agrega un criterio nuevo.

### E4 — P-C

La salida preservada declara para los SHAs congelados:

- N_CONSTRUCTOR: `rev-list --count = 5`; enumeración única = `5`;
- N_AUDITOR: `rev-list --count = 7`; enumeración única = `7`.

El código muestra que ambas derivaciones operan sobre los SHAs congelados y compara `git rev-list --count` con una enumeración por `git log --format=%H` deduplicada.

No se observa divergencia en la evidencia preservada.

E4 queda satisfecho dentro de la limitación congelada sobre esas historias.

### Controles N1-N9

La salida literal enumera N1-N9 como exigidos y presentes, y los casos correspondientes producen rechazo o fail-closed según su expectativa.

No se observa aceptación de N1-N9.

Esto no neutraliza F3: el contrato exige además E3.

## Resultado de la corrida congelada

VEREDICTO_CORRIDA=FALLO

Razón:

- E1 satisfecho para el corpus ejecutado;
- E2 satisfecho;
- **E3 no satisfecho**;
- E4 satisfecho;
- **F3 ocurrió**;
- N1-N9 no fueron aceptados.

Como el éxito exigía E1-E4 simultáneamente y F3 es criterio explícito de fallo, el `VEREDICTO=EXITO` escrito por el verificador no es aceptable como interpretación del AUDITOR.

El contrato queda agotado por esta corrida. No puede reutilizarse para una segunda ejecución.

## Defecto adicional del candidato descubierto al interpretar la evidencia

### D-01 — DISEÑO — BLOQUEANTE

El candidato §2 declara que el orquestador toma el último bloque `json` de la salida.

El corpus convierte esa regla en un comportamiento observable mediante C02:

- construye una salida con **dos bloques JSON**;
- espera `RECIBIDO`;
- la corrida marca C02 como `OK`.

La implementación confirma esa conducta: `extraer()` usa `rfind("'''json")` conceptualmente sobre el marcador final y no rechaza la existencia de un bloque JSON previo.

Eso contradice la autoridad superior de REVOLUTIONS §4.2, que establece:

- la respuesta termina con un único bloque `json`;
- ese bloque es **el único bloque JSON de la respuesta**;
- no existe contenido posterior.

REVOLUTIONS explica además que el consumidor toma el último bloque porque la unicidad elimina la necesidad de elegir entre varios candidatos, no porque varios bloques sean válidos.

CT-7 no tiene autoridad para redefinir `revolutions-hop/v1` ni su forma de respuesta.

RIESGO_OPERACIONAL=Dado el comportamiento actual, un actor que incumpla la forma autoritativa emitiendo un bloque JSON adicional podría ser aceptado y transportado en lugar de provocar fail-closed. El verificador actual incluso certifica esa aceptación como correcta.

D-01 bloquea el cierre de U2.

## Veredicto de U2

VEREDICTO=U2_NO_APROBADA_CORRIDA_FALLIDA_Y_DEFECTO_DE_DISENO

U2 permanece abierta.

No corresponde transición a U3.

No existe necesidad humana: ambas propiedades están dentro de la autoridad técnica del CONSTRUCTOR para corregir y del AUDITOR para volver a evaluar.

## Condición para la próxima verificación

La corrección del candidato cambiará su blob. En consecuencia, el contrato congelado y agotado ya no aplicará.

Antes de ejecutar cualquier nueva verificación discriminante debe existir un nuevo contrato previo conforme a REVOLUTIONS §6.1 y PLAN §5.2, evaluado y congelado por AUDITOR.

Ese nuevo contrato debe demostrar, sin prescribir aquí el diseño del mecanismo:

1. que la forma de respuesta que el candidato hace aceptar/rechazar es compatible con REVOLUTIONS §4.2, incluida la unicidad del bloque JSON y la ausencia de contenido posterior;
2. que la afirmación de cobertura E3 alcanza realmente a **todas las reglas mecánicas del candidato**, incluida la secuencia de arranque externo, y no sólo a una lista de reglas cuyo conjunto pueda omitir silenciosamente materia del propio candidato;
3. que las demás propiedades congeladas de U2 continúan discriminándose, incluidos N1-N9 y P-C.

## Próxima acción

PROXIMA_ACCION=Corregir dentro de U2 el candidato respecto de D-01 y corregir la insuficiencia de cobertura que activó F3. Actualizar EVENTO.md y proponer un nuevo contrato previo para el nuevo blob del candidato, con una propiedad que demuestre la forma de respuesta compatible con REVOLUTIONS §4.2 y una cobertura E3 realmente referida a todas las reglas mecánicas del candidato, incluida la secuencia de arranque externo. No ejecutar ninguna mitad de una nueva verificación antes de que el AUDITOR evalúe y congele ese nuevo contrato. Cerrar esa intervención mediante un único commit autoritativo y devolver su RESULT_WORK_SHA.

DECISION_HUMANA_OBTENIDA=NINGUNA_EN_ESTA_INTERVENCION
