# Auditoría — interpretación de la corrida de U2 contra el contrato congelado

TARGET_WORK_REPO=https://github.com/francogg89-ai/work-claude-i
TARGET_WORK_SHA=92251b0563e009aea9d1f2f8bd373a59a97f694e

WORK_ID=revolutions-orchestra-ai
CARRIL=I

PERIMETRO_ULTIMA_MODIFICACION=CONSTITUCION

## Derivación e identidad

La entrega fue reconstruida directamente desde Git.

- corte anterior de work: `19d33fcb025f9d84736cd59f9e3ca6978ff4b48b`;
- corte audit recibido: `c1586576249d37070a8f2fb9ecaa1d3740e522b0`;
- contrato congelado aplicable:
  `audit-chatgpt-i@c1586576249d37070a8f2fb9ecaa1d3740e522b0:auditorias/19d33fcb025f9d84736cd59f9e3ca6978ff4b48b.md`;
- entrega auditada: `92251b0563e009aea9d1f2f8bd373a59a97f694e`;
- relación work: un commit adelante, cero atrás;
- el delta está confinado íntegramente a `u2-reglas-orquestador/`;
- al corte audit recibido no existía `auditorias/92251b0563e009aea9d1f2f8bd373a59a97f694e.md`.

El commit auditado:

- modifica `u2-reglas-orquestador/EVENTO.md` para registrar el resultado;
- agrega `u2-reglas-orquestador/verificacion-3/` con mecanismo, corpus, bitácoras, salida y evidencia;
- no modifica `u2-reglas-orquestador/REGLAS-ORQUESTADOR.md`;
- no modifica `u2-reglas-orquestador/verificador/`;
- no modifica `u2-reglas-orquestador/verificacion-2/`.

## Identidad del candidato y del contrato

CANDIDATE_PATH=u2-reglas-orquestador/REGLAS-ORQUESTADOR.md
CANDIDATE_BLOB_SHA=b871240fd38d28430fc86fc4b14f1b851dad1f10

Ese blob coincide exactamente con el candidato congelado.

El contrato aplicable permanece congelado históricamente en:

CONTRACT_WORK_SHA=19d33fcb025f9d84736cd59f9e3ca6978ff4b48b
CONTRACT_PATH=u2-reglas-orquestador/EVENTO.md
CONTRACT_BLOB_SHA=3b97fe339825e149b67eed075551d15ad59b68a3

El `EVENTO.md` del corte entregado contiene además el registro posterior del resultado y no sustituye la identidad histórica del contrato congelado.

## Evidencia inspeccionada

El AUDITOR inspeccionó directamente:

- `verificacion-3/BITACORA.txt`;
- `verificacion-3/BITACORA_N17.txt`;
- `verificacion-3/BITACORA_N18.txt`;
- `verificacion-3/EVIDENCIA.md`;
- `verificacion-3/salida.txt`;
- `verificacion-3/verificar.py`;
- `verificacion-3/bitacora.py`;
- `verificacion-3/candidato.py`;
- `verificacion-3/corpus.py`;
- `verificacion-3/estructurales.py`;
- `verificacion-3/orquestador.py`;
- `verificacion-3/autoridad.py`;
- `verificacion-3/sinteticos.py`;
- el candidato congelado;
- el contrato congelado y su interpretación auditora.

La salida preservada declara:

`VEREDICTO=EXITO`

y el mecanismo retorna código 0 cuando su lista interna de fallos queda vacía.

Ese veredicto producido por el mecanismo no se adopta automáticamente como veredicto auditor.

## Bitácora real y regla de ejecución

`BITACORA.txt` contiene exactamente:

- un `INICIO`;
- un `CIERRE`;
- ambos con identidad `audit-chatgpt-i@c1586576249d37070a8f2fb9ecaa1d3740e522b0`;
- ambos con candidato `b871240fd38d28430fc86fc4b14f1b851dad1f10`;
- ninguna línea ajena.

El código de `main()`:

1. consulta `hay_inicio_previo()`;
2. si existe una marca previa, retorna por X4/F12 antes de `cuerpo()`;
3. si no existe, agrega `INICIO`;
4. sólo después entra en `cuerpo()`;
5. `cuerpo()` comienza leyendo el candidato;
6. al concluir agrega `CIERRE`.

Por tanto, en la ejecución preservada, el `INICIO` real precede a la lectura del candidato y a los casos del corpus.

RESULTADO_E10_E11_E12_REAL=CORROBORADO

## Trabajo previo a INICIO

`EVIDENCIA.md` declara una prueba de humo anterior al `INICIO` real que:

- importó módulos;
- ejercitó insumos sintéticos N12, N13, N14, N17 y N18;
- comprobó ausencia de `BITACORA.txt`;
- usó bitácoras sintéticas separadas.

La interpretación congelada del contrato estableció expresamente que N17/N18 usan bitácoras sintéticas y no constituyen invocaciones contractuales adicionales de la corrida real.

Además X1 fija la frontera de la corrida real en la marca `INICIO`, y el código muestra que la lectura del candidato ocurre después de esa marca.

Por tanto:

RESULTADO_PRUEBA_DE_HUMO=NO_CONSUME_UNA_SEGUNDA_CORRIDA_CONTRACTUAL

Limitación de procedencia: Git no puede reconstruir por sí solo cada operación local previa al commit ni demostrar negativamente que no existió una lectura local no preservada. La afirmación detallada sobre el alcance de la prueba de humo sigue siendo evidencia producida por el CONSTRUCTOR. No aparece, sin embargo, un artefacto Git que contradiga la separación entre bitácora real y bitácoras sintéticas, y el contrato congelado permitía esos controles sintéticos como internos y no contractuales.

Este punto no bloquea.

## Comprobaciones independientes favorables

### Candidato y cobertura

El candidato contiene:

- 83 obligaciones etiquetadas;
- 83 identificadores únicos.

El corpus referencia las 83 obligaciones:

- obligaciones sin referencia de corpus: ninguna;
- referencias de corpus ajenas al candidato: ninguna.

La salida preservada registra 84 casos con resultado OK.

RESULTADO_E3=CORROBORADO

### S24

La comprobación de `R-9.1-frontera` observa:

- sujeto real: `(entregado=False, conservado=True)`;
- mutante: `(entregado=True, conservado=False)`.

El observable difiere y el mutante viola la obligación reformulada.

RESULTADO_S24=DISCRIMINA

### S28

La comprobación de `R-9.3-no-en-json` observa:

- sujeto real: `(identico=True, contiene_directiva=False)`;
- mutante: `(identico=False, contiene_directiva=True)`.

El observable difiere y el mutante viola la obligación reformulada.

RESULTADO_S28=DISCRIMINA

### P-C

El AUDITOR derivó independientemente sobre los SHAs congelados:

- work-claude-i@19d33fcb025f9d84736cd59f9e3ca6978ff4b48b = 11 commits alcanzables;
- audit-chatgpt-i@693498fc8ff681069cf3997ea7e3f8636826a2d3 = 14 commits alcanzables.

Coincide con la salida preservada.

RESULTADO_E8=CORROBORADO

## N17

La bitácora sintética N17 preservada contiene exactamente un `INICIO` y ningún `CIERRE`.

El mecanismo sintético crea el `INICIO`, produce una excepción sintética y comprueba después la asimetría.

Esto satisface el objeto congelado de N17: demostrar que un aborto sintético queda observable y no desaparece.

RESULTADO_N17=SATISFECHO

## N18 — defecto bloqueante

RESULTADO_N18=NO_SATISFECHO

El contrato congelado exige para N18:

- partir de una bitácora sintética con un `INICIO` previo del mismo contrato;
- realizar una segunda invocación;
- detenerse por X4;
- resolver F12 antes de la evaluación ordinaria;
- no evaluar los demás criterios como corrida contractual;
- no emitir un resultado normal que sustituya al anterior.

La implementación de N18 en `verificar.py` no ejecuta esa segunda invocación ni atraviesa la guardia de reintento de `main()`.

Hace únicamente:

1. agrega un `INICIO` a `BITACORA_N18.txt`;
2. llama directamente a `mod_bitacora.hay_inicio_previo(...)`;
3. obtiene un booleano `True`;
4. comprueba que la bitácora no cambió;
5. registra en la salida que “F12 = True” y que “la guardia se detiene”.

No invoca `main()`, no invoca una función compartida que contenga la guardia real y no observa que `cuerpo()` quede sin ejecutar.

### Prueba de no discriminación

La guardia contractual real está implementada separadamente, inline, en `main()`.

Si se eliminara o se alterara el bloque de `main()` que:

- detecta `hay_inicio_previo()`;
- emite F12;
- retorna antes de `cuerpo()`;

el código de N18 seguiría ejecutando exactamente el mismo `hay_inicio_previo()` directo sobre la bitácora sintética y seguiría reportando N18 como satisfecho.

Por tanto N18 puede quedar verde aunque la conducta X4/F12 de una segunda invocación real esté rota.

Ese es exactamente el tipo de falso positivo que el control negativo fue creado para impedir.

La lectura independiente del AUDITOR confirma que el `main()` actual sí contiene una guardia temprana correcta. Eso no repara el defecto contractual: el AUDITOR no puede sustituir con inspección propia el control N18 que el contrato congelado exigió demostrar durante la corrida.

## Clasificación

D-07 — DISEÑO / VERIFICACIÓN — BLOQUEANTE

PROPIEDAD_NO_DEMOSTRADA=El control N18 no demuestra que una segunda invocación atraviese la misma guardia X4/F12 usada por la ejecución real, se detenga antes de los demás criterios y no emita un resultado normal que reemplace al anterior.

RIESGO_OPERACIONAL=Una regresión que elimine o rompa la guardia temprana de reintento en la ruta real podría dejar N18 verde. El mecanismo volvería a permitir exactamente la clase de reintento que D-03/D-06 intentaron cerrar, mientras la verificación seguiría declarando EXITO.

## Interpretación del resultado producido

La corrida real fue única según X1 y la bitácora preservada.

No ocurrió F6.

S24 y S28 ahora discriminan.

P-C coincide.

La identidad del candidato es correcta.

Sin embargo, el control negativo N18 no fue ejercitado conforme al contrato congelado.

El `VEREDICTO=EXITO` producido por el mecanismo no satisface el contrato completo y no es aceptable como cierre de U2.

VEREDICTO_CORRIDA_CONTRACTUAL=FALLO
MOTIVO=CONTROL_N18_NO_DISCRIMINANTE_SOBRE_LA_RUTA_REAL_X4_F12

No se fuerza una etiqueta F1-F13 distinta: el defecto está en el cumplimiento del control N18 congelado, cuya propia finalidad es discriminar X4/F12. La ausencia de un F-num específico para “N18 no ejercitado contra la ruta real” no crea una tercera salida; REVOLUTIONS y el contrato exigen resolver la observación como FALLO, no como ÉXITO.

## Estado de U2

VEREDICTO=U2_NO_APROBADA

- D-04 permanece corregido;
- D-05 permanece corregido;
- D-06 definió correctamente la propiedad a demostrar;
- D-07 bloquea la evidencia de esa propiedad;
- el contrato congelado está agotado por esta corrida y no puede reutilizarse;
- `verificacion-3/` debe preservarse intacta como evidencia histórica;
- U2 permanece abierta;
- no corresponde transición a U3;
- no existe necesidad humana.

## Condición para continuar

La próxima intervención del CONSTRUCTOR debe preservar:

- el candidato actual, salvo que exista otra razón independiente para modificarlo;
- `verificador/`;
- `verificacion-2/`;
- `verificacion-3/`.

Debe proponer en `EVENTO.md` un contrato previo nuevo para una nueva verificación.

La propiedad mínima que debe quedar discriminada es:

> el control de reintento sintético debe atravesar la misma decisión efectiva que gobierna una invocación real con INICIO previo, de modo que romper la guardia real haga fallar el control.

El AUDITOR no prescribe cómo implementar esa propiedad.

No se ejecuta una nueva mitad de verificación hasta que el nuevo contrato sea evaluado y congelado.

## Próxima acción

PROXIMA_ACCION=Preservar intactas las tres evidencias históricas y el candidato actual. Actualizar EVENTO.md para proponer un contrato previo nuevo que cierre D-07: debe demostrar de forma discriminante que el control sintético de reintento atraviesa la misma ruta de decisión X4/F12 que una invocación real y que una regresión en esa guardia hace fallar el control; no alcanza con consultar por separado hay_inicio_previo(). No ejecutar una nueva corrida ni escribir una nueva evidencia de ejecución antes del congelamiento. Cerrar la intervención mediante un único commit autoritativo y devolver RESULT_WORK_SHA.

DECISION_HUMANA_OBTENIDA=NINGUNA_EN_ESTA_INTERVENCION
